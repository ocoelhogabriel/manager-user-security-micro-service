eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka
  instance:
    prefer-ip-address: true
server:
  port: 8082
spring:
  application:
    name: microauth
  config:
    import: "configserver:"
